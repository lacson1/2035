# Fly.io Environment Variables Example
# Set these using: flyctl secrets set KEY=VALUE

# Database (Required)
# Get from: flyctl postgres attach
# Or use external: Supabase, Neon, etc.
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# JWT Secrets (Required)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here-replace-with-32-char-string
JWT_REFRESH_SECRET=your-refresh-secret-here-replace-with-32-char-string

# JWT Expiry (Optional - defaults shown)
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (Required)
# Set to your frontend URL
CORS_ORIGIN=https://your-frontend.vercel.app

# Redis Cache (Optional - improves performance 60-85%)
# Get from: https://upstash.com (free tier available)
REDIS_URL=redis://default:password@host:6379

# Rate Limiting (Optional)
RATE_LIMIT_WINDOW_MS=900000        # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100        # 100 requests per window

# Node Environment (Auto-set by Fly.io)
NODE_ENV=production
PORT=3000

# Optional: Seed database on first deployment
# SEED_DATABASE=true

# Optional: Sentry for error tracking
# SENTRY_DSN=https://your-sentry-dsn
